# Core Flask dependencies
flask>=2.0.0
flask-cors>=3.0.10

# NLP and ML models
# Note: spaCy model 'en_core_web_sm' is downloaded during deployment (see render.yaml)
spacy>=3.5.0
transformers>=4.30.0
torch>=2.0.0

# Testing and utilities
pytest>=7.0.0
pytest-timeout>=2.1.0
matplotlib>=3.5.0
numpy>=1.21.0

# Server and APIs
gunicorn>=21.0.0
groq>=0.4.0
python-dotenv>=0.19.0